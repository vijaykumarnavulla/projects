import { MydiretiveDirective } from './mydiretive.directive';

describe('MydiretiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MydiretiveDirective();
    expect(directive).toBeTruthy();
  });
});
