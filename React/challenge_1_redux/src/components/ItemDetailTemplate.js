import React from "react";
var isExpand = false;
function handleChange() {
  // alert("hi");
}
const ItemDetailTemplate = ({ props, toggleTodo }) => (
  <div>
    <span onClick={this.handleChange}>Hide Item details -</span>
  </div>
);
export default ItemDetailTemplate;
